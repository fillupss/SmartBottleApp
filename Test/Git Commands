Command Line code

ls: Checks which files are within the directory
cd "Directory": goes to the directory (use tab to fill in directory name after typing first few characters)
cd .. : goes back to the previous directory

git status: checks which branch you are working on and compares the one on github to present work
git branch -a: checks which branch you are working on and shows all branches
git checkout -b <new branch>: creates a new branch <new branch> and prepares to transfer files from the current working branch to the new branch
git add * -f: adds the file to the current branch being worked on (excluding -f will yield a warning)
git commit -m "message": commits to the local branch with a message
git pull: pulls everything from the repo
git push -u origin philip: pushes the commit to the branch "philip" where there is a branch upstream to the "origin"